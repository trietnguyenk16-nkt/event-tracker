import {NextRequest,NextResponse} from 'next/server'; import {z} from 'zod'; import {db} from '@/lib/db'; const schema=z.object({title:z.string().trim().min(1).max(200),description:z.string().max(5000).optional().nullable(),event_datetime:z.coerce.date(),tags:z.array(z.string()).max(20).default([]),is_completed:z.boolean().default(false),reminder_offset_minutes:z.number().int().min(0).max(10080).nullable().optional(),email:z.string().email().optional().nullable().or(z.literal(''))}); export async function GET(){return NextResponse.json(await db.event.findMany({orderBy:{event_datetime:'asc'}}))} export async function POST(req:NextRequest){try{return NextResponse.json(await db.event.create({data:schema.parse(await req.json())}),{status:201})}catch{return NextResponse.json({error:'Dữ liệu không hợp lệ'},{status:400})}}